name: build

# Builds the standalone LabBench app on every push to main (after PR
# merge). Artifacts are attached to the workflow run and downloadable
# for 30 days. For a proper release, push a ``v*`` tag and this same
# workflow creates a GitHub Release with the binaries attached.
#
# Runs in parallel on three OS runners:
#   macOS (arm64)   → LabBench.app + .dmg   (Apple Silicon)
#   Windows x64     → LabBench.exe bundle    (.zip)
#   Linux   x64     → LabBench directory     (.tar.gz)

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:   # manual trigger from the Actions tab

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write     # required to publish GitHub Releases on tag pushes

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macos-arm64
            runner: macos-14        # Apple Silicon runner
            artifact: LabBench-macos-arm64
            archive: zip
          - name: windows-x64
            runner: windows-latest
            artifact: LabBench-windows-x64
            archive: zip
          - name: linux-x64
            runner: ubuntu-latest
            artifact: LabBench-linux-x64
            archive: tar

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Linux-only Qt runtime libs
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-sync1 \
            libxcb-xfixes0 libxcb-xinerama0 libxcb-xkb1 libxkbcommon-x11-0 \
            libegl1 libgl1 libglib2.0-0 libdbus-1-3

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-build.txt

      - name: Run tests (headless)
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m unittest discover -s tests -v

      - name: Build app
        env:
          QT_QPA_PLATFORM: offscreen
        run: python scripts/build_app.py --clean

      - name: Package macOS .app into .zip
        if: runner.os == 'macOS'
        run: |
          cd dist
          zip -r LabBench-macos-arm64.zip LabBench.app

      - name: Package Windows bundle into .zip
        if: runner.os == 'Windows'
        run: |
          cd dist
          Compress-Archive -Path LabBench -DestinationPath LabBench-windows-x64.zip

      - name: Package Linux bundle into .tar.gz
        if: runner.os == 'Linux'
        run: |
          cd dist
          tar -czf LabBench-linux-x64.tar.gz LabBench

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            dist/LabBench-*.zip
            dist/LabBench-*.tar.gz
          if-no-files-found: error
          retention-days: 30

  release:
    # Only runs when a v* tag is pushed. Collects the three OS
    # artifacts and publishes them on a GitHub Release.
    name: Publish release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          draft: false
          prerelease: ${{ contains(github.ref_name, '-alpha') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-rc') }}
          generate_release_notes: true
          files: |
            artifacts/**/LabBench-*.zip
            artifacts/**/LabBench-*.tar.gz
